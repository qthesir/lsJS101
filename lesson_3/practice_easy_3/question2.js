// What will the following code output?

console.log([1, 2, 3] + [4, 5]);

// Answer without running

/* 
This is just going to be a concatenated string value, without the brackets. That is, the arrays will be 
coherced to strings by the + operator.
*/